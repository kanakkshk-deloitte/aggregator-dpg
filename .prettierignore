node_modules/
dist/
.next/
*.tsbuildinfo
pnpm-lock.yaml
CHANGELOG.md
openapi.json
# Helm chart templates use Go-template syntax (`{{ ... }}`) inside YAML,
# which prettier's YAML parser cannot parse. Skip the templates dir.
helm/**/templates/**
